name: farmrent
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  permission_handler: ^12.0.1
  provider: ^6.1.1
  url_launcher: ^6.3.2
  image_picker: ^1.2.2
  google_generative_ai: ^0.4.7

  # Firebase suite grouped neatly together with updated stable versions
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.4
  firebase_storage: ^12.4.0
  cloud_firestore: ^5.6.0
  http: ^1.6.0
  razorpay_flutter: ^1.4.5
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
